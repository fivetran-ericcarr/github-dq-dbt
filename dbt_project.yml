name: 'github_dq'
version: '1.0.0'
config-version: 2

profile: 'default'

model-paths: ["models"]
target-path: "target"
clean-targets: ["target", "dbt_packages"]

models:
  github_dq:
    staging:
      +materialized: view
      +schema: staging
    marts:
      +materialized: table
      +schema: marts
